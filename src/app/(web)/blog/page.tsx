import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog page",
  description: "This is blog page",
};

export default function BlogPage() {
  return (
    <div>
      <h1>Hello Page Blog</h1>
    </div>
  );
}
